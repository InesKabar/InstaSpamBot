# InstaSpamBot
# Python GUI Automation

A small Python automation project using PyAutoGUI to automatically type text from a `text.txt` file.

## Requirements

- Python
- PyAutoGUI

## Usage

Run `main.py` and select the target application within the 5-second delay.
